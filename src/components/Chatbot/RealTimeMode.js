import { Card } from "../ui/card";

const RealTimeMode = () => {
  return (
    <Card className="px-3 rounded-full py-1 bg-orange-400 font-semibold text-white text-sm mt-1">
      Real Time
    </Card>
  );
};

export default RealTimeMode;
